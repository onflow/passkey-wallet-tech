import React, { PropsWithChildren } from "react";
type DarkModeContextType = {
    isDark: boolean;
};
export declare function DarkModeProvider({ children, darkMode, }: PropsWithChildren<{
    darkMode?: boolean;
}>): React.JSX.Element;
export declare function useDarkMode(): DarkModeContextType;
export {};
