import React, { PropsWithChildren } from "react";
import { FlowConfig } from "../core/context";
import { QueryClient } from "@tanstack/react-query";
import { createFlowClient } from "@onflow/fcl";
import { Theme } from "../core/theme";
export type ColorMode = "light" | "dark" | "system";
interface FlowProviderProps {
    config?: FlowConfig;
    queryClient?: QueryClient;
    flowClient?: ReturnType<typeof createFlowClient>;
    flowJson?: Record<string, any>;
    theme?: Partial<Theme>;
    colorMode?: ColorMode;
}
export declare function FlowProvider({ config: initialConfig, queryClient: _queryClient, flowClient: _flowClient, flowJson, theme: customTheme, children, colorMode, }: PropsWithChildren<FlowProviderProps>): React.JSX.Element;
export {};
