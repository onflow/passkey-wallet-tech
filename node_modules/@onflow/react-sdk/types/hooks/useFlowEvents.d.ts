import { Event } from "@onflow/typedefs";
import { useFlowClient } from "./useFlowClient";
export interface EventFilter {
    startBlockId?: string;
    startHeight?: number;
    eventTypes?: string[];
    addresses?: string[];
    contracts?: string[];
    opts?: {
        heartbeatInterval?: number;
    };
}
export interface UseFlowEventsArgs extends EventFilter {
    /** Called for each new event received */
    onEvent: (event: Event) => void;
    /** Optional error callback */
    onError?: (error: Error) => void;
    flowClient?: ReturnType<typeof useFlowClient>;
}
/**
 * useFlowEvents hook
 *
 * Subscribes to a Flow event stream and calls the provided callbacks.
 */
export declare function useFlowEvents({ startBlockId, startHeight, eventTypes, addresses, contracts, opts, onEvent, onError, flowClient, }: UseFlowEventsArgs): void;
